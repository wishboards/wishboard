dist
coverage
reports
.stryker-tmp
.aws-sam
node_modules
src/client/node_modules
src/client/public
package-lock.json
*.ttf
*.log

# Managed by release-please; its changelog formatting isn't Prettier-compatible.
CHANGELOG.md

# Astro's generated type/content cache. Prettier only reads this top-level
# ignore file, not docs/.gitignore, so building the docs locally would otherwise
# make `npm run format:check` fail on generated files.
.astro/
